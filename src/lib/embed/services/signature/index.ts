/**
 * Signature validation services
 * 
 * Provides browser-compatible cryptographic signature validation
 * for validating requests from iframe plugins before data transformation.
 */

export { FrontendSignatureValidator } from './FrontendSignatureValidator';
export type { SignableData } from './FrontendSignatureValidator'; 